# Prompt

## Refinement
This list is updated as soon as the stuff works, only the next few points (TODOs) are listed
- lock the data base / app after 10 min inactivity. Do an autosave first if I'm on the entry page.
- use the same colors on the calender as on the list of entries. Use the list of entries colors as reference.
- remove the legend of the color coding below the calendar, instead add an average number on the month overview. Use two digits after the point and display the avarage only if there are entries in this month, otherwise display a placeholder. Place it below the calendar.
- add a reminder when last db export is older than 30 days. Display a text about the data being stored only locally and biometrically encrypted (data is useless whenever fingerprints should change).
- shorten the text on the list of entries. Have the date and the start of the text only on one line.
- fix the edge-to-edge warning in publish store (tbd: copy warning)
- try out some different UIs / colors. TODO: maybe with another tool (figma)
- have the lock symbol more prominent (TODO: decide how)


---
- Does not work, probably Android/Samsung specific: 
  on the Sperrscreen: always have the 3 standard android buttons visible (back/home/overview)
